# Server-Authoritative Economy

เกมนี้แบ่งเป็น 2 ส่วนที่ชัดเจน:

## 1. Combat/Wave แบบ real-time — ยังอยู่ฝั่ง client (เหมือนเดิม)
การต่อสู้จริง (ตำแหน่งซอมบี้, กระสุน, HP ระหว่างเฟรม, แอนิเมชัน) ยังคำนวณและวาดที่เบราว์เซอร์ผู้เล่นแบบ
60fps เหมือนเดิมทุกประการ — การทำให้ตรงนี้เป็น server-side เต็มรูปแบบต้องออกแบบเป็นเกม multiplayer-grade
ใหม่หมด (server tick, state sync, lag compensation) ซึ่งเป็นงานคนละขนาดกับเกมนี้

## 2. เศรษฐกิจ/ความคืบหน้า — ย้ายไป server ทั้งหมดแล้ว (จุดที่โกงได้ในเวอร์ชันเดิม)
ทุกอย่างที่มีผลต่อ "ของที่ผู้เล่นครอบครองจริง" ต้องผ่าน API และ **server เป็นคนตัดสินผลลัพธ์ทั้งหมด**
ไคลเอนต์ทำได้แค่ "ขอ" ไม่สามารถกำหนดผลลัพธ์เองได้:

| การกระทำ | เดิม (คำนวณที่ client) | ตอนนี้ |
|---|---|---|
| เงิน/เพชรที่ได้ตอนจบเวฟ | คำนวณและบวกเข้า `money` ตรงๆ ในเบราว์เซอร์ | client บอกแค่ "จบเวฟแล้ว" → **server** คำนวณจากข้อมูลเวฟที่ตัวเองออกให้ตั้งแต่แรกเท่านั้น |
| จำนวน/HP/ความเร็วซอมบี้ต่อเวฟ | client คำนวณเอง (แก้ไขในเบราว์เซอร์ได้เลย) | `POST /api/wave/start` — **server** เป็นคนสร้างและเก็บไว้ผูกกับผู้เล่น |
| ซื้อ/อัพเกรด/ขายป้อม | หัก/บวกเงินตรงๆ ที่ client | server ตรวจเงิน+คลังป้อมก่อนทุกครั้ง ไม่พอ/ไม่ถูกต้อง = ปฏิเสธ |
| กาชา | สุ่มที่ client (เปิด devtools แก้ผลได้) | **server สุ่มเอง 100%** client รู้ผลตอน response กลับมาเท่านั้น |
| อีโว | ตัด/เพิ่มคลังป้อมที่ client | server ตรวจจำนวนป้อมที่ต้องใช้ก่อนทุกครั้ง |
| ปลดล็อกสกิน/ธีมแมพ | client เช็คเงื่อนไขเอง | server คำนวณจาก bestWave/kill/level ที่เก็บจริงใน DB เท่านั้น |

**เงิน/เพชรที่โชว์ระหว่างเล่น (ตอนฆ่าซอมบี้) เป็นแค่ preview** — คำนวณด้วยสูตรเดียวกับ server เพื่อความ
ลื่นไหล แต่พอเวฟจบจริง ตัวเลขจะถูก "เขียนทับ" ด้วยค่าจาก server เสมอ (`applyFullGameData`) ต่อให้มีคน
แก้ตัวแปรในเบราว์เซอร์ระหว่างเล่น ก็จะถูกแก้กลับเป็นค่าจริงทันทีที่เวฟจบ

## Auth
ระบบ PIN-based เหมือน unit-battle: ตั้งชื่อ + PIN (4-6 หลัก) ตอนเล่นครั้งแรก (`POST /api/register`)
ครั้งต่อไปล็อกอินด้วยชื่อ+PIN เดิม (`POST /api/login`) ได้ session token เก็บใน localStorage
(`tvz_token`) แนบไปกับทุก request เป็น `Authorization: Bearer <token>`

PIN แฮชด้วย `crypto.scrypt` (built-in ของ Node ไม่ต้องพึ่ง bcrypt) เก็บแค่ hash+salt ไม่เก็บ PIN ตรงๆ

## ตั้งค่า Database บน Railway
1. เปิด Railway project → **New** → **Database** → **Add PostgreSQL**
2. Railway จะตั้งตัวแปร `DATABASE_URL` ให้ service ของเกมอัตโนมัติ (ไม่ต้องกรอกเอง ถ้าอยู่ project เดียวกัน)
3. Deploy service เกม — ตอน start จะรัน `backend/db/schema.sql` สร้างตารางให้อัตโนมัติถ้ายังไม่มี (ดู `backend/db/init.js`)

รันทดสอบในเครื่อง: ต้องมี Postgres รันอยู่ (หรือใช้ Docker) แล้ว export `DATABASE_URL` ก่อน `npm start`
```bash
export DATABASE_URL="postgres://user:pass@localhost:5432/tvz"
npm install
npm start
```

## ขอบเขตที่ยัง "เชื่อ client" อยู่ (รู้ไว้เผื่อขยายต่อ)
- **ผลของการต่อสู้** (ยิงโดน/ไม่โดน, ป้อมตาย, เวลาที่ใช้ในเวฟ) ยังเป็นการจำลองฝั่ง client — คนที่แก้โค้ด
  เบราว์เซอร์ตัวเองยังทำให้ "จบเวฟไว/ง่ายกว่าที่ควร" ได้ (เช่น เรียก `/wave/complete` ทันทีโดยไม่รอสู้จริง)
  แต่ **จะไม่ทำให้ได้เงิน/ป้อม/สกินเกินสิทธิ์** เพราะรางวัลคำนวณจากเวฟที่ server ออกให้จริง ไม่ใช่จากผล
  การต่อสู้ที่ client รายงานมา — จุดที่เสียหายสูงสุด (การได้ทรัพยากรมาฟรีๆ) ถูกปิดแล้ว ส่วนที่เหลือคือ
  "เล่นข้ามด่านไว" ซึ่งกระทบแค่ตัวเอง ไม่ใช่ dupe/แฮ็กทรัพยากร
- เพิ่มการเช็ค **เวลาขั้นต่ำต่อเวฟ** แล้ว (`backend/routes/wave.js`) — เรียก `/wave/complete` เร็วเกินกว่า
  จำนวนศัตรูในเวฟจะเป็นไปได้จะโดนปฏิเสธ ผลข้างเคียงคือปุ่ม dev "⚡ ชนะเวฟทันที" ก็โดนกฎนี้ด้วยเหมือนกัน
  (ถ้ากดรัวๆ ติดกันจะเจอ error ชั่วคราว) ถือว่าตั้งใจ เพราะ dev tool ก็ควรผ่านการตรวจสอบเดียวกับผู้เล่นจริง
  ถ้าอยากได้ dev-only bypass จริงๆ (ข้ามการเช็คนี้เฉพาะตอนทดสอบ) บอกได้ จะเพิ่ม flag แยกให้
