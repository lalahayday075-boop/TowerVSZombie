# รายงานตรวจโค้ด — Tower vs Zombie (ก่อนรื้อทำใหม่)

## 🗑️ ไฟล์ตายแล้ว / ไม่ได้ใช้ (ลบได้เลย)
- `ฮindex.html`, `ฮindex 1.html` — โปรโตไทป์เก่า อ้างอิงไฟล์ `js/TOWER_TYPES.js`, `js/drawtw.js` ที่ไม่มีอยู่จริงแล้ว ไม่ได้ถูกเปิดใช้งานจริง (`index.html` คือตัวจริง)
- `wavedata.js` — ใช้เฉพาะใน `ฮindex.html` เก่า ไม่ได้ถูกโหลดใน `index.html`
- root `zombie.js` — ไฟล์ว่างเปล่า (0 บรรทัดจริง) ถูกแทนที่ด้วย `zombie/zombie/zombie.js`
- `skin/drawtwskins.js` — ไฟล์ว่างเปล่า แต่ยัง `<script>` โหลดอยู่ใน index.html โดยไม่มีผลอะไร
- `respawnTower(slot)` ใน `tower.js` — ฟังก์ชันไม่เคยถูกเรียกใช้จากที่ไหนเลย และถ้าถูกเรียกจะพังทันที (อ้างตัวแปร `type` ที่ไม่มีอยู่จริงในสโคป ต้องเป็น `slot.towerType`)
- `evolveStage` field บน Tower — ถูก set ตอนสร้าง (=0), ถูกเซฟ/โหลด แต่ไม่เคยถูกเปลี่ยนค่าที่ไหนเลย ระบบอีโวจริงใช้การเปลี่ยน `type` เป็น `_evo1` แทน ทำให้โค้ดใน `getMaxLevel()` ที่เช็ค `evolveStage === 1/2` เป็น dead code ตลอดกาล
- ไฟล์คำนวณ/เครื่องมือแยก (`คำนวณเงินรวม1ป้อม.html`, `คำนวณเงินได้เมื่อถึงเวฟ.html`, `ใส่ข้อมูลเคสวาดรูป.html`) — เป็นเครื่องมือช่วย dev ไม่ใช่ส่วนของเกม จะไม่ย้ายเข้าโปรเจกต์ใหม่ (เก็บแยกไว้ให้ถ้าต้องการ)

## 🐞 บั๊กที่เจอจริง

1. **สกินป้อม "obsidian" ทำให้เกมพังทันทีที่ใช้งาน (ร้ายแรงสุด)** — `drawskins.js` มีเคส `case "obsidian": drawNormalObsidian(ctx, tower);` แต่ **ไม่มีฟังก์ชัน `drawNormalObsidian` อยู่จริงในโปรเจกต์เลยสักไฟล์เดียว** สกินนี้ปลดล็อกอัตโนมัติแค่ผ่านเวฟ 10 (เงื่อนไขง่ายมาก) ถ้าผู้เล่นคนไหนสลับไปใช้สกินนี้ ตอนวาดป้อมจะเกิด `ReferenceError` ทันที ซึ่งจะทำให้ลูปเกมทั้งหมด (requestAnimationFrame) หยุดทำงาน เกมค้าง/พังทั้งเกม ไม่ใช่แค่ป้อมตัวเดียว — ในเวอร์ชันใหม่ให้ fallback ไปใช้ลายเส้นปกติ (default) แทนชั่วคราวจนกว่าจะมีลายจริง กันเกมพัง

2. **EXP popup ไม่เคยแสดงผล** (`Damage.js`) — `spawnDamage()` เขียนเป็น ternary chain ที่พลาด: บรรทัด `source === "exp" ? "#00ccff" : damagePopups.push(...)` ทำให้เวลา source เป็น `"exp"` โค้ดจะ "คำนวณสี" ทิ้งเฉยๆ แล้ว **ไม่ push popup เลย** — ป้อบอัป `+EXP` ที่ตั้งใจจะโชว์ตอนฆ่าซอมบี้เลยไม่เคยขึ้นจริง
3. **เงินเริ่มต้นผิดปกติ / เกิน safe-integer ของ JS** (`index.html`) — `let money = 9999999999999999999;` (และ diamonds ก็เช่นกัน) ค่านี้เกิน `Number.MAX_SAFE_INTEGER` ทำให้เลขคลาดเคลื่อนได้ทันทีที่บวก/ลบ ดูเหมือนเป็นค่าทดสอบที่ลืมเอาออกก่อนโหมดจริง (HTML เองก็ยัง hardcode ข้อความเริ่มต้นเป็น "100" อยู่)
4. **`DEV_MODE = true`** ยังเปิดอยู่ในโค้ดที่ดูเหมือนจะปล่อยจริง ทำให้ปุ่ม DEV (เสกป้อม/เสกซอมบี้/เพิ่มเงินไม่จำกัด) โผล่ในเกมเสมอ
5. ~~Tower ใช้สี muzzle ไม่ตรงกับที่ตั้งใจ~~ — ตรวจซ้ำแล้วไม่ใช่บั๊ก: `drawXxxBySkin(ctx, this)` ส่ง tower instance จริงเข้าไป ฟังก์ชันวาด set `tower.muzzleColor` บน instance นั้นตรงๆ ทุกเฟรมก่อนยิง ทำงานถูกต้องตามที่ตั้งใจ (คงพฤติกรรมเดิมไว้ในเวอร์ชันใหม่)
6. **DEV_TOWER_IMMORTAL ป้องกันไม่ครบ** — โหมด "ป้อมอมตะ" เช็คใน `Zombie.update()` (melee) เท่านั้น แต่ `ZombieBullet` (ยิงไกล) และ `ZombieLightning` (สายฟ้า) หักเลือดป้อมตรงๆ โดยไม่เช็คธงนี้เลย ป้อมยังโดนซอมบี้ยิงตายได้แม้เปิดโหมดอมตะ
7. **ตารางรางวัลเวฟมีช่วงที่พุ่งผิดปกติแล้วร่วงกลับ** (`wave/WaveConfig.js`, `REWARD_TABLE`) — เรียงตามเวฟ: 1-5:15-20, 6-10:17-23, 11-20:20-25, **21-40:110-160**, 41-60:16-30, 61-90:30-60, 91-120:60-120, ... จะเห็นว่าทุกช่วงค่อยๆ ไต่ขึ้นแบบราบเรียบ (ตั้งแต่ 41-60 เป็นต้นไปคูณ ~2 เท่าทุกช่วง) ยกเว้นช่วง 21-40 ที่ให้ 110-160 พุ่งสูงกว่าช่วงก่อนและหลังมาก แล้วร่วงกลับลงมาที่ 16-30 ในช่วงถัดไป — คือช่วงที่ผิดปกติ (น่าจะพิมพ์ตัวเลขผิด) ในเวอร์ชันใหม่ปรับให้ช่วง 21-40 ไล่เรียงต่อเนื่อง (22-28) แทน ถ้าอยากได้ค่าพุ่งช่วงนี้ตามเดิมแจ้งได้ ปรับกลับให้ได้ทันที
8. **ฟังก์ชัน `openSettings` / `closeSettings` ถูกประกาศซ้ำสองรอบ** ใน `settingsUI.js` (บรรทัดท้ายทับของเดิม) เป็นโค้ดซ้ำซ้อนที่ไม่มีผลต่อพฤติกรรม แต่สร้างความสับสน/เสี่ยงแก้ไขพลาดในอนาคต
9. **สกินซอมบี้ "Songkran" ทำให้ซอมบี้บางแบบล่องหน** (`zombie/zombieskin/drawzombies.js` + `drawzombieSongkran.js`) — `drawzombies.js` ประกาศฟังก์ชันตัวช่วย (stub) เปล่าไว้ 5 ตัว เช่น `function drawGroundRangedSongkran(ctx, z) { }` เผื่อไฟล์หลังจะมาทับ ปรากฏว่ามีแค่ 3 ใน 5 ที่ถูกทับด้วยของจริงจาก `drawzombieSongkran.js` (`drawGroundMeleeSongkran`, `drawGroundRangedSongkran`, `drawAirMeleeSongkran`) ส่วน **`drawGroundLightningSongkran`, `drawAirRangedSongkran`, `drawAirLightningSongkran` ยังเป็นฟังก์ชันเปล่าอยู่** และระบบ fallback ใน `renderZombie()` เช็คแค่ "มีฟังก์ชันหรือไม่" (ฟังก์ชันเปล่าก็ถือว่า "มี") เลยไม่ fallback ไปใช้สกินปกติให้ ผลคือถ้าผู้เล่นใส่สกินซอมบี้ Songkran แล้วเจอซอมบี้ประเภท ground_lightning / air_ranged / air_lightning จะ **มองไม่เห็นตัวซอมบี้เลย** (ยังตีป้อมได้ปกติ แค่วาดไม่ขึ้น) — แก้โดยให้ 3 ประเภทนี้ fallback ไปใช้ลายเส้นปกติ (default) แทนจนกว่าจะมีลายเฉพาะจริง

10. **`ZombieBullet.update()`** คำนวณ `targetVx/targetVy` (ให้กระสุนไล่เป้าหมายที่ขยับ) แต่ไม่เคยเอาไปใช้จริง (ใช้ `this.vx/vy` ที่ fix มุมตอนยิงแทน) เป็นโค้ดตายที่ทำให้เข้าใจผิดว่ากระสุนไล่เป้าได้

10. **สกินซอมบี้ "toxic" ปลดล็อกได้แต่ใช้ไม่ได้จริง** (`renderZombie.js`) — ที่ 300 kill สะสม ระบบจะ unlock สกิน `"toxic"` เข้า `playerData.zombieSkin.owned` แต่ไม่มีการวาด (`zombieSkinRenderers`) ของสกินนี้อยู่เลย เมนูเลือกสกินก็อ่านจาก `zombieSkinRenderers` เป็นหลักเลยไม่โผล่ให้เลือกด้วย กลายเป็นฟีเจอร์ที่ปลดล็อกลมๆ แล้งๆ ไม่มีผลอะไร — เอาเงื่อนไขปลดล็อกนี้ออกไปก่อนจนกว่าจะมีลายจริง

11. **พรีวิวป้อมหมุนใน popup รั่วไหลสะสมเรื่อยๆ** (`UIt.js`, `animateTowerPreview`) — ทุกครั้งที่เปิด popup ร้านค้า/กาชา จะสร้าง `<canvas>` ใหม่พร้อม `requestAnimationFrame` loop ของตัวเอง แต่ปิด popup แล้ว **loop เหล่านี้ไม่เคยถูกหยุด** เพราะ canvas เก่าถูกลบออกจาก DOM ไปแล้วแต่ลูปยังวาดใส่ canvas ที่ไม่มีใครเห็นต่อไปเรื่อยๆ เปิด-ปิด popup บ่อยๆ จะสะสม loop ซ้อนกันมากขึ้นเรื่อยๆ ทำให้เกมช้าลงเมื่อเล่นไปนานๆ — แก้โดยเช็ค `canvas.isConnected` ทุกเฟรม ถ้า canvas ถูกลบไปแล้วให้หยุด loop ทันที

## 🔁 โครงสร้างที่ซ้ำซ้อนโดยไม่จำเป็น

- **ทุกป้อมมีคู่ base/`_evo1` ที่ต้องพิมพ์ค่าสเตตัสซ้ำทั้งหมด** ใน `TOWER_TYPES` (เช่น `normal` กับ `normal_evo1` ค่าตัวเลขเหมือนกันทุกตัว ต่างแค่ `maxLevel`) — เสี่ยงมากที่แก้ค่าฝั่งหนึ่งแล้วลืมอีกฝั่ง ในเวอร์ชันใหม่จะรวมเป็นนิยามเดียวแล้วให้ evolve เป็นการ "ต่อยอด"ค่า ไม่ใช่การก็อปปี้
- **สวิตช์ `switch(this.type)` เพื่อเลือกฟังก์ชันวาด ถูกก็อปปี้อยู่ 2 ที่เป๊ะๆ** (`tower.js` ใน `Tower.draw()` และ `UIt.js` ใน `animateTowerPreview()`) — รวมเป็นตารางเดียว (map แบบ `TOWER_MUZZLE`) แล้วใช้ร่วมกัน
- **ระบบ state กระจายอยู่ในตัวแปร global กว่า 30 ตัว** ใน `<script>` ของ `index.html` (money, wave, towers, zombies, buildSlots ฯลฯ) ทุกไฟล์เข้าถึง/แก้ตรงๆ ได้หมด ทำให้ debug ยากและเสี่ยงชนกัน — เวอร์ชันใหม่รวมเป็น `state.js` ก้อนเดียว
- **การเซฟข้อมูลผู้เล่นเก็บซ้ำ 2 ที่**: `playerData` เซฟเองผ่าน `savePlayerData()` (localStorage key `tvz_player`) และยังถูกฝังซ้ำอยู่ใน `getFullGameData()` (key `tvz_fullsave`) ถ้าจังหวะเซฟไม่ตรงกันข้อมูลสองก้อนอาจไม่ตรงกัน (race เล็กๆ) — เวอร์ชันใหม่รวมเป็นแหล่งเดียว

## ✅ แนวทางแก้ไขในเวอร์ชันใหม่
- รวม state ทั้งหมดไว้ใน module เดียว (`src/core/state.js`) แทน global variables
- นิยามป้อมแบบไม่ซ้ำ: เก็บ base stats ครั้งเดียว + evolve เป็น modifier
- แก้บั๊กทั้ง 8 ข้อข้างต้นตรงจุด (พฤติกรรมเกมจะเหมือนเดิมทุกอย่าง ยกเว้นจุดที่เป็นบั๊กชัดเจน)
- ปรับเงินเริ่มต้นเป็นค่าปกติ (100 ตามที่ UI ตั้งใจไว้) และปิด `DEV_MODE` เป็น default false — ถ้าต้องการคงค่าเดิมไว้สำหรับทดสอบ แจ้งได้ จะย้อนกลับให้
